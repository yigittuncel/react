import React from "react";
import './Button.css'

const Button = (props) => {
  return (
    <div>
      <button type='submit' className="button">Add User</button>
    </div>
  )
}

export default Button;
